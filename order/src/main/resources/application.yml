spring:
  application:
    name: order
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  cloud:
    bus:
      enabled: true
server:
  port: 8081
rabbitmq:
  queue:
    order:
      name: order
  exchange:
    order:
      name: order
  binding:
    order:
      name: order
